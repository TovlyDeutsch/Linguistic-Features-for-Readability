corpus:
  name: WeeBit
  reprocess: False

k_fold: 5

subsample_splits: 10

short_name: Cnn ordered class regression fed word vectors + SVM

runner_type: gpu

features:
  - name: Neural
    model_config: configs/WeeBit/regressionOrderedCnn.yaml
    reprocess: True


model: 
  name: svm
