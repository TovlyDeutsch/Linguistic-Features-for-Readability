corpus:
  name: WeeBit
  reprocess: False

k_fold: 5

subsample_splits: 10

short_name: Cnn age regression fed word vectors + SVM

runner_type: gpu

features:
  - name: Neural
    model_config: configs/WeeBit/regressionCnn.yaml
    reprocess: True


model: 
  name: svm
