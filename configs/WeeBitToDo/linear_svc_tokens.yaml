corpus:
  name: WeeBit
  reprocess: False

k_fold: 5

subsample_splits: 10

short_name: Linear regression (SVC) classification with word types

runner_type: cpu_multi

features:
  - name: WordTypes
    reprocess: False

model: 
  name: LinearSVC
